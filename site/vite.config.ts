import { defineConfig } from 'vite'

export default defineConfig({
  // Cambiar a '/' para dominio custom
  // Si vuelves a usar buildersaqp.github.io/api-comida-peru, cambiar a '/api-comida-peru/'
  base: '/',
})
